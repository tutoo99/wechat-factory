# 发布素材

- 标题：我把公众号排版从一个 `theme.yaml` 拆成了 5 个文件，多套风格这次算是接住了
- 摘要：原来我以为“排版单调”只是多加几个 theme 的事，结果一动手才发现，真正该升级的不是样式，而是主题模型本身。这次我把公众号排版系统从单文件 `theme.yaml` 升级成 schema v1，顺手打通了多风格扩展、主题推荐和最终自动渲染这条链路。
- 导语：这次升级比较值钱的，不是多了两个主题，而是后面那条路现在能继续走了。
- 封面主标题：排版系统重构
- 封面副标题：从 `theme.yaml` 到 schema v1
