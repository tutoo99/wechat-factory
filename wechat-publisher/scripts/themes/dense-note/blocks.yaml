body_text:
  enabled: true
  variant: default
  align: justify

lead_text:
  enabled: true
  variant: spacious
  font_size_delta: 1

section_title:
  enabled: true
  variant: badge-left-light
  icons:
    - "01"
    - "02"
    - "03"
    - "04"
    - "05"
    - "06"
  rotate_colors: true

sub_title:
  enabled: true
  variant: plain-bold

quote_card:
  enabled: true
  variant: soft-panel

checklist:
  enabled: true
  variant: clean-bullets

step_list:
  enabled: true
  variant: numbered-flow

code_panel:
  enabled: true
  variant: wechat-card
  show_language_label: true

data_table:
  enabled: true
  variant: compact-grid

image_block:
  enabled: true
  variant: plain

divider:
  enabled: true
  variant: thin-line

summary_box:
  enabled: true
  variant: border-callout
